export interface FlightData {
  origin: string;
  destination: string;
  departureTime: string;
  arrivalTime: string;
  price: string;
  id: string;
  currency: string;
  airlines: string;
}

export interface FormData {
  origin: string;
  destination: string;
  departureDate: string;
}